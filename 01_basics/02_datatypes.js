"use strict"; // treat all js code as per newer version of js standard.

// alert("Good Morning :)"); // Error is occurred as we are using node.js instead of browser.

let name = "Vineet";
let age = 25;
let isLoggedIn = true;
let city;

// number => 2**53
// string => '' or ""
// bigint
// boolean => true or false
// null => it is a standalone value, it is a representation of an empty value
// undefined => we have just declared a variable and not assigned any value, so it holds undefined in memory
// symbol => unique
// object

console.log(typeof "london"); // string
console.log(typeof 100); // number 
console.log(typeof null); // object => it is an error in js
console.log(typeof undefined); // undefined